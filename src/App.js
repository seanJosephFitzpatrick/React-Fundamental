import React from "react";

function App() {
  return (
    <div className="App">
      <a href="https://reactjs.org/docs/introducing-jsx.html">React</a>
      <h1>This funny tag syntax is neither a string nor HTML.</h1>
      <p>
        It is called JSX, and it is a syntax extension to JavaScript. We
        recommend using it with React to describe what the UI should look like.
        JSX may remind you of a template language, but it comes the full power
        of JavaScript.
      </p>
    </div>
  );
}

export default App;
